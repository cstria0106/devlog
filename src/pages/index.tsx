import * as React from "react";
import DefaultLayout from "../layouts/default";

const IndexPage = () => {
  return (
    <DefaultLayout>
      <h1>Wecome!</h1>
    </DefaultLayout>
  );
};

export default IndexPage;
